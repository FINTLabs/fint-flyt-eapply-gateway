apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: nfk-no

resources:
  - ../../../base

labels:
  - pairs:
      app.kubernetes.io/instance: fint-flyt-eapply-gateway_nfk_no
      fintlabs.no/org-id: nfk.no

patches:
  - patch: |-
      - op: replace
        path: "/spec/kafka/acls/0/topic"
        value: "nfk-no.flyt.*"
      - op: replace
        path: "/spec/orgId"
        value: "nfk.no"
      - op: add
        path: "/spec/imagePullSecrets"
        value:
          - ghcr
      - op: replace
        path: "/spec/ingress/basePath"
        value: "/nfk-no/api/eapply"
      - op: replace
        path: "/spec/probes/startup/path"
        value: "/nfk-no/actuator/health"
      - op: replace
        path: "/spec/probes/readiness/path"
        value: "/nfk-no/actuator/health/readiness"
      - op: replace
        path: "/spec/probes/liveness/path"
        value: "/nfk-no/actuator/health/liveness"
      - op: add
        path: "/spec/env/-"
        value:
          name: novari.kafka.topic.org-id
          value: "nfk-no"
      - op: add
        path: "/spec/env/-"
        value:
          name: server.servlet.context-path
          value: "/nfk-no"
    target:
      kind: Application
      name: fint-flyt-eapply-gateway
  - patch: |-
      - op: add
        path: "/spec/itemPath"
        value: "vaults/aks-api-vault/items/fint-flyt-eapply-oauth2-client-nfk-no"
    target:
      kind: OnePasswordItem
      name: fint-flyt-eapply-oauth2-client
