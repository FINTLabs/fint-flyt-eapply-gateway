name: CI Build

on:
    pull_request:
    push:
        branches:
            - main

jobs:
    build:
        uses: FINTLabs/fint-flyt-github-workflows/.github/workflows/java-app-build-image.yaml@bump-action
        with:
            push-image: ${{ github.event_name == 'push' }}
        secrets: inherit
