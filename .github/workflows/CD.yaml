name: CD Deploy

on:
    workflow_run:
        workflows:
            - CI Build
        types:
            - completed

env:
    REGISTRY: ghcr.io

permissions:
    contents: read
    packages: read

jobs:
    deploy-to-aks:
        if: >
            github.event.workflow_run.conclusion == 'success' &&
            github.event.workflow_run.head_branch == 'main'

        runs-on: ubuntu-latest

        strategy:
            matrix:
                org:
                    - fintlabs-no

                cluster:
                    - aks-beta-fint-2021-11-23

        steps:
            - name: Checkout source
              uses: actions/checkout@v7

            - name: Derive lowercase image name
              run: echo "IMAGE_NAME=$(echo "$GITHUB_REPOSITORY" | tr '[:upper:]' '[:lower:]')" >> "$GITHUB_ENV"

            - name: Determine image reference
              id: image
              run: echo "image=${REGISTRY}/${IMAGE_NAME}:${{ github.event.workflow_run.head_sha }}" >> "$GITHUB_OUTPUT"

            - name: Log in to GHCR
              uses: docker/login-action@v4
              with:
                  registry: ${{ env.REGISTRY }}
                  username: ${{ github.actor }}
                  password: ${{ secrets.GITHUB_TOKEN }}

            - name: Pull image
              run: docker pull ${{ steps.image.outputs.image }}

            - name: Determine environment
              id: environment
              uses: actions/github-script@v9
              with:
                  script: return '${{ matrix.cluster }}'.split('-')[1];
                  result-encoding: string

            - name: Determine resource group
              id: resource-group
              uses: actions/github-script@v9
              with:
                  script: return 'rg-aks-${{ steps.environment.outputs.result }}';
                  result-encoding: string

            - name: Set deployment image
              run: |
                  sed -i "s#^\([[:space:]]*image:\).*#\1 ${{ steps.image.outputs.image }}#" kustomize/base/flais.yaml
                  grep -q "image: ${{ steps.image.outputs.image }}" kustomize/base/flais.yaml

            - name: Bake manifests
              id: bake
              uses: azure/k8s-bake@v4
              with:
                  renderEngine: kustomize
                  kustomizationPath: kustomize/overlays/${{ matrix.org }}/${{ steps.environment.outputs.result }}

            - name: Verify baked image
              run: grep -R "${{ steps.image.outputs.image }}" "${{ steps.bake.outputs.manifestsBundle }}"

            - name: Azure login
              uses: azure/login@v2
              with:
                  creds: ${{ secrets[format('AKS_{0}_FINT_GITHUB', steps.environment.outputs.result)] }}

            - name: Install kubelogin
              uses: azure/use-kubelogin@v1
              with:
                  kubelogin-version: 'v0.2.12'

            - name: Set AKS context
              uses: azure/aks-set-context@v5
              with:
                  cluster-name: ${{ matrix.cluster }}
                  resource-group: ${{ steps.resource-group.outputs.result }}
                  admin: true
                  use-kubelogin: true

            - name: Deploy to AKS
              uses: azure/k8s-deploy@v5.0.3
              with:
                  action: deploy
                  manifests: ${{ steps.bake.outputs.manifestsBundle }}
                  images: ${{ steps.image.outputs.image }}
                  namespace: ${{ matrix.org }}
